[gd_scene load_steps=4 format=2]

[ext_resource path="res://resources/levels/freescifiplatform/png/Objects/DoorOpen.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Area2D

# Declare member variables here. Examples:
signal enteredEndDoor

# Called when the node enters the scene tree for the first time.
func _ready():
	set_process(true)
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	check_area()
	pass


func check_area():
	var area = self.get_overlapping_bodies()
	if (area.size() != 0):
		for body in area:
			if (body.is_in_group(\"character\")):
				emit_signal(\"enteredEndDoor\")"

[sub_resource type="RectangleShape2D" id=2]
extents = Vector2( 14.002, 24.7784 )

[node name="OpenDoor" type="Area2D" groups=[
"end_door",
]]
position = Vector2( 1.00006, 0 )
scale = Vector2( 2, 3 )
script = SubResource( 1 )

[node name="Sprite" type="Sprite" parent="."]
scale = Vector2( 0.1, 0.1 )
texture = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2( 1, 0.285985 )
scale = Vector2( 1, 0.789861 )
shape = SubResource( 2 )
