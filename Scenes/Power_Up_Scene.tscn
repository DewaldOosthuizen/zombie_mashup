[gd_scene load_steps=4 format=2]

[ext_resource path="res://icon.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]

script/source = "extends KinematicBody2D

# variables
var playerSpeedY = 0
var velocity = Vector2(0, 0)
var movementDirection = 1

const PLAYERSPEEDX = 100
const GRAVITY = 800

func _ready():
	set_process(true)
	pass
	
	
func _process(delta):
	_move(delta)
	pass


func _move(delta):
	playerSpeedY += GRAVITY * delta
	velocity.x = 0
	velocity.y = playerSpeedY * delta
	var collider = move_and_collide(Vector2(1, 1))
	#move_local_x(velocity.x)
	
	pass"

[sub_resource type="RectangleShape2D" id=2]

custom_solver_bias = 0.0
extents = Vector2( 32.1838, 32.7688 )

[node name="PowerUp" type="KinematicBody2D" groups=[
"PowerUp",
]]

scale = Vector2( 0.6, 0.6 )
input_pickable = false
collision_layer = 1
collision_mask = 1
collision/safe_margin = 0.08
script = SubResource( 1 )
_sections_unfolded = [ "Transform" ]

[node name="Sprite" type="Sprite" parent="." index="0"]

texture = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="." index="1"]

shape = SubResource( 2 )


